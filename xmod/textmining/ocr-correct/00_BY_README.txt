
nlookup-pre-0.4:

first experiments


nlookup-0.4:

minimum edit distance, using ranking


nlookup-0.5

n-grams, using ranking

as the n-grams method is really a similarity measure, unlike the
distance measure of the minimum edit distance method, the "inverse" of
the similarity value is used to get a distance value that can be
compared with the distance value from nlookup-0.4


